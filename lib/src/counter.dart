num add(num first, num second) => first + second;
num subtract(num first, num second) => first - second;
num multiply(num first, num second) => first * second;
num divide(num first, num second) => first / second;
